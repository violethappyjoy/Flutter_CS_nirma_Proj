import 'package:flutter/material.dart';

class HomeTitle extends StatelessWidget {
  const HomeTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Home Page",
      style: TextStyle(
          decoration: TextDecoration.none,
          fontSize: 35.0,
          fontFamily: 'SofiaSansCondensed',
          fontWeight: FontWeight.w700),
    );
  }
}

class ProfileTitle extends StatelessWidget {
  const ProfileTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Profile",
      style: TextStyle(
          decoration: TextDecoration.none,
          fontSize: 35.0,
          fontFamily: 'SofiaSansCondensed',
          fontWeight: FontWeight.w700),
    );
  }
}

class LoginTitle extends StatelessWidget {
  const LoginTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Login",
      style: TextStyle(
          decoration: TextDecoration.none,
          fontSize: 35.0,
          fontFamily: 'SofiaSansCondensed',
          fontWeight: FontWeight.w700),
    );
  }
}

class SignupTitle extends StatelessWidget {
  const SignupTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Sign-Up",
      style: TextStyle(
          decoration: TextDecoration.none,
          fontSize: 35.0,
          fontFamily: 'SofiaSansCondensed',
          fontWeight: FontWeight.w700),
    );
  }
}

class ToDo extends StatelessWidget {
  const ToDo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "To-Do",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class NameBar extends StatelessWidget {
  const NameBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Name ",
      style: TextStyle(
        decoration: TextDecoration.none,
        fontSize: 35,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w700,
      ),
    );
  }
}

class CurrProj extends StatelessWidget {
  const CurrProj({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Current Project",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class CheckInOut extends StatelessWidget {
  const CheckInOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Check-In and Check-Out",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class LeavePlan extends StatelessWidget {
  const LeavePlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Leave Plan",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class ArchiveF extends StatelessWidget {
  const ArchiveF({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Archives",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class NewsLetter extends StatelessWidget {
  const NewsLetter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "News Letter",
      style: TextStyle(
        color: Colors.white,
        decoration: TextDecoration.none,
        fontSize: 25,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w400,
      ),
    );
  }
}

class LoginText extends StatelessWidget{
  const LoginText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Login",
      style: TextStyle(
        decoration: TextDecoration.none,
        fontSize: 35,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w700,
      ),
    );
  }

}

class SignupText extends StatelessWidget{
  const SignupText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Signup",
      style: TextStyle(
        decoration: TextDecoration.none,
        fontSize: 35,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w700,
      ),
    );
  }

}

class AdminText extends StatelessWidget{
  const AdminText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Admin",
      style: TextStyle(
        decoration: TextDecoration.none,
        fontSize: 35,
        fontFamily: 'SofiaSansCondensed',
        fontWeight: FontWeight.w700,
      ),
    );
  }

}